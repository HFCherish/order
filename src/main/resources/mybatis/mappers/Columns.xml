<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="columns">
    <sql id="user">
        U.id as userId,
        U.name as userName
    </sql>

    <sql id="product">
        P.id as prodId,
        P.name as prodName,
        P.description as prodDesc,
        P.price as prodPrice,
        P.rating as prodRate
    </sql>

    <sql id="order">
        O.id as orderId,
        O.name as userName,
        O.address as userAddr,
        O.phone as userPhone,
        O.created_at as createDate,
        OI.product_id as prodId,
        OI.quantity as prodQuantity,
        OI.amount as prodAmount
    </sql>

</mapper>
